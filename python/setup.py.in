from setuptools import setup, find_packages

setup(name="dune.vem",
    namespace_packages=['dune'],
    description="Python lib for dune: virtual element implementation",
    version="2.4",
    author="Andreas Dedner and Martin Nolte",
    packages = find_packages(),
    zip_safe = 0,
    package_data = {'': ['*.so']}
  )
